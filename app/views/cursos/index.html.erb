<main class="content">
  <header class="intro-header" style="background-image: url(https://d2lm6fxwu08ot6.cloudfront.net/img-thumbs/960w/J7YRJBKV9D.jpg)">
      <div class="header-container">
        <div class="row">
          <h1> Coloque seus projetos em Prática!</h1>
          <h3>Aprenda marketing digital e progração de uma forma que você pode começar a aplicar ainda hoje!</h3>
      </div>
    </div>
  </header>
  <div class="clearfix"></div>
  <section id="index-subheader">
    
  </section>
  <section id="index-courses">
    <div class="container">
      <h1 style="text-align: center; margin-bottom: 20px;"> CURSOS </h1>
          <% @cursos.in_groups_of(3) do |group| %>
          <div class="row">
          <% group.each do |curso| %>
            <% if curso %>
              <div class="col-md-4 col-sm-6 col-xs-12">
                  <div class="panel panel-default index-panel">
                      <!--<%= image_tag(curso.icon, class:'circleimgindex center') %>-->
                    <%= link_to curso do %>
                    <h3><%= curso.name %></h3>
                    <% end %>
                    <p class="index-course-content"><%= truncate(curso.content, :length => 110) %></p>
                    <hr>
                    <div class="row">
                      <div class="col-xs-6 center">
                        <b><%= curso.aulas.sum(:time)/360 %>h</b> de curso
                      </div>
                      <div class="col-xs-6 center">
                        <b><%= curso.users.count %></b> Inscritos
                      </div>
                    </div>
                    <button class="btn btn-highlight" > COMEÇAR AGORA!</button>
                  <% end %>
                  </div>
                </div>
            <% end %>
           </div>
          <% end %>
          </div>
      </div>
  </div>
-->
</main>
